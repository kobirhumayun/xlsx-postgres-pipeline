# Security & Environment

## Authentication
- No auth required
- Optional basic auth toggle is allowed

## Secrets & server boundary
- Never expose DB credentials to client
- Use server-only environment variables
- Provide `.env.example` and clear setup steps